import React from 'react';
import { ExampleComponent } from 'test-react-library';

const App: React.FC = () => (
   <div>
      <ExampleComponent text="Modern React component module" />
   </div>
);

export default App;
